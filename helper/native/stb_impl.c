/* stb_image_write implementation — compiled separately to avoid
 * multiple-definition issues when the header is included elsewhere. */
#define STB_IMAGE_WRITE_IMPLEMENTATION
#include "stb_image_write.h"
